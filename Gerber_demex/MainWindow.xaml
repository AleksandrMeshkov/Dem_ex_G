<Window x:Class="Gerber_demex.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gerber_demex"
        mc:Ignorable="d"
        Title="Новые технологии" Height="450" Width="800"
        WindowStartupLocation = "CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="#BBDCFA">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="49*"/>
                <ColumnDefinition Width="351*"/>
            </Grid.ColumnDefinitions>
            <Image HorizontalAlignment="Left" 
                   Margin="30,0,0,0" 
                   Source="/img/Новые технологии.png"/>

            <Grid Grid.Column="1" HorizontalAlignment="Right">
                <Button x:Name="btBack" 
                Content="Назад" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Center" 
                Click="ClickBack" 
                Width="150" 
                Height="35"
                Margin="0,0,30,0"
                Visibility="Collapsed">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#0C4882" 
                            CornerRadius="5">
                                <TextBlock Text="{TemplateBinding Content}" 
                                    Foreground="White"
                                    FontFamily="Bahnschrift Light SemiCondensed"
                                    FontSize="14"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
            <Grid Grid.Column="1" HorizontalAlignment="Center">
                <TextBlock x:Name="Title" 
                       HorizontalAlignment="Center" 
                       Foreground="#0C4882" 
                       FontFamily="Bahnschrift Light SemiCondensed" 
                       FontSize="24" 
                       FontWeight="ExtraBold" 
                       Margin="0,0,100,0"
                       VerticalAlignment="Center"/>
            </Grid>
        </Grid>

        <Frame x:Name="FrameCont" 
               Grid.Row="1" 
               NavigationUIVisibility="Hidden" 
               ContentRendered="FrameCont_ContentRendered" 
               Navigated="frContent_Navigated"/>

    </Grid>
</Window>